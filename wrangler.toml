# Cloudflare Pages static site (Next.js static export).
# Deploy with: npm run deploy:pages (uses CLOUDFLARE_PAGES_PROJECT_NAME; default: wealthtracker)

name = "wealthtracker"
pages_build_output_dir = "./out"
