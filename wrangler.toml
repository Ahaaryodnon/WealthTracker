# Cloudflare Pages static site (Next.js static export).
# Deploy with: npx wrangler pages deploy out --project-name=wealthtracker
# (Passing "out" explicitly ensures CI finds the directory; config is fallback.)

name = "wealthtracker"
pages_build_output_dir = "./out"
