# Cloudflare Pages static site (Next.js static export).
# Deploy with: npx wrangler pages deploy out --project-name=inequalitycalculator
# (Project name must match the Pages project in your Cloudflare dashboard.)

name = "inequalitycalculator"
pages_build_output_dir = "./out"
